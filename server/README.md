A simple shorterner service built with Node.js and Express.
It saves whatever data is sent to it and serves it back when requested.
A unique ID is generated for each saved item, which is used to retrieve it later.